<%= form_with(model: material, local: true) do |form| %>
  <% if material.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(material.errors.count, "error") %> prohibited this material from being saved:</h2>

      <ul>
      <% material.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :material_name %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount, id: :material_amount %>
  </div>

  <div class="field">
    <%= form.label :descrp %>
    <%= form.text_area :descrp, id: :material_descrp %>
  </div>

  <div class="field">
    <%= form.label :nr_inv %>
    <%= form.number_field :nr_inv, id: :material_nr_inv %>
  </div>

  <div class="field">
    <%= form.label :supplier %>
    <%= form.text_field :supplier, id: :material_supplier %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
